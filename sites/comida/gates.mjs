// Commercial gates (plan/P0-ANSWERS.md). A gated line is built but stays noindex, out of the
// sitemap and out of navigation until its partner is signed. Flip to true only with a signed partner.
export default {
  chefPartnerSigned: false,
  viandasPartnerSigned: false,
};
