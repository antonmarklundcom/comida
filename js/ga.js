/* GA4 init as a file (not inline) so the Content-Security-Policy needs no 'unsafe-inline' for scripts. */
window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments)}
gtag('js',new Date());gtag('config',document.currentScript.getAttribute('data-id'));
