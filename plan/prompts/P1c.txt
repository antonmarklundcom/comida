You are building comida.com.py, a static Spanish (Paraguay, voseo) lead site for catering and chef a domicilio in Gran Asuncion. Repo root: C:\Claude 1\comida-com-py. Read, in this order, plan/08-V2-DECISIONS.md, plan/04-BUILD-SPEC.md, plan/03-OFFER-AND-FUNNEL.md, plan/02-SEO-AND-SITEMAP.md, AGENTS.md, then plan/prompts/P1.txt which holds the full numbered task list. Where files disagree, 08 wins, then 04. If plan/P0-ANSWERS.md exists use its values; if not, use the placeholders OPERADOR_PENDIENTE, +595000000000 and the CRM URL from AGENTS.md, and list every placeholder under Assumptions. Production is HTML/CSS/JS plus one PHP handler on Hostinger; Node runs only on this laptop as build tooling. Never drop, narrow or substitute a step: run it and flag, or stop and ask. Never print, echo or write secrets.

Forbidden in any rendered text: 24 h, 24 horas, 2 a 3 presupuestos, verificados, garantizado, todo Paraguay, any price number, testimonials, partner names, review counts, logos.

This dispatch is P1c. Do tasks 8, 9 and the .htaccess part of task 11 of plan/prompts/P1.txt. The PHP handler must follow every rule in plan/04 section 5: absolute private config path constant outside public_html documented in README, server-side field allowlist, phone normalisation to +595 format, request size limit, file-based rate limit, idempotency key, timeout, JSON response, success only on confirmed CRM persistence, WhatsApp fallback with the brief encoded on any failure, no public log, no hard-coded key. Add public/js/form-mock.js used only when the page is served from localhost so the form flow can be tested in the static preview. Write docs/php-tests.md with the five Hostinger test cases: success, rejected payload, upstream timeout, duplicate submission, missing config.

Commands to run before reporting: build and verify with --site=comida, both exit 0; php -l on lead-forward.php if php is on PATH, otherwise flag it. Grep dist for any string that looks like a key or token and confirm none.

Definition of done: report under 250 words: files created or changed, verify summary, php -l result or flag, Assumptions, Flagged. Do not paste file contents.
