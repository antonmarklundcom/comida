You are building comida.com.py, a static Spanish (Paraguay, voseo) lead site for catering and chef a domicilio in Gran Asuncion. Repo root: C:\Claude 1\comida-com-py. Read, in this order, plan/08-V2-DECISIONS.md, plan/04-BUILD-SPEC.md, plan/03-OFFER-AND-FUNNEL.md, plan/02-SEO-AND-SITEMAP.md, then plan/codex-input/CODEX-CRITIQUE.md section 9. Where files disagree, 08 wins, then 04. If something is still missing, choose the simplest option and list it under Assumptions in your report.

Production is HTML/CSS/JS plus one PHP handler on Hostinger. Node runs only on this laptop as build tooling.

Task P1 (launch set, 20 indexable routes + 404):

1. Copy C:\Claude 1\prestamo-com-py\engine into engine/. Keep its loader model: create sites/comida/site.config.mjs and the modules it expects, and make every build and verify call use --site=comida. Make these deliberate changes to the engine: add comida to the allowed site IDs; allow phone, WhatsApp, hours and operator legal fields in config; make blog generation optional and disable it for comida; remove insurance and loan copy, categories, disclaimers and the loan calculator; copy public/ and php/ into dist/comida.com.py/ during build.
2. In engine/verify.mjs remove the bans on forms, consent controls, VenderCRM references, PHP output and Offer schema. Keep escaping, routing and identity checks. Add every rule listed in plan/04 section 8.
3. Create sites/comida/routes.mjs as the single route manifest with id, kind, path, title, meta, indexable and published for exactly the routes in plan/08 section Launch route manifest. Sitemap must list only indexable published routes: gracias and 404 excluded. The chef-a-domicilio route has published true and indexable false and is hidden from navigation until the config flag chefPartnerSigned is true.
4. Create sites/comida/prices.mjs with the internal reference entries from plan/01 section 4, every entry with published false, plus the fields unit, minGuests, minCharge, tax, transport, inclusions, source and verifiedAt. Nothing with published false may render a number anywhere.
5. Create occasions.mjs, cities.mjs (asuncion only), lines.mjs (chef), menus.mjs and faq.mjs using the content envelope in plan/04 section 3. Every related id must resolve to a published route or the build fails.
6. Templates: home, hub, occasion, city, line, precios, form, como-funciona, proveedores, sobre, contacto, terminos, privacidad, gracias (noindex), 404.
7. Write full Spanish copy (voseo) for all 20 routes. Use the gated launch promise from plan/03 section 1; never write 24 horas, 2 a 3 presupuestos, verificados, todo Paraguay, or any price number. Each occasion page: unique intro, formats, que incluye, 3 sample menus, 6 to 8 unique FAQ as visible HTML, related links, a prefilled wa.me message. Precios page explains what drives cost for 50 and 100 personas and por persona, with the sentence Depende del menu, la cantidad de personas y el servicio. No testimonials, partner names, review counts or logos.
8. Implement the two-screen form from plan/03 section 2 and php/lead-forward.php per plan/04 section 5: absolute private config path outside public_html read from a constant documented in README.md, server-side field allowlist, phone normalisation, request size limit, file-based rate limit, idempotency key, timeout, and a JSON response. Success only on confirmed CRM persistence. On any failure the page shows the WhatsApp fallback with the brief encoded. Never write a public log. Never hard-code a key. Add a supplier form on proveedores posting to the same handler with leadType proveedor.
9. Implement the estimator on precios only, per plan/04 section 4; it must render cotizacion personalizada while no published price exists.
10. JSON-LD per plan/04 section 7: Organization and WebSite on home, Service and BreadcrumbList on occasion, city and line pages, BreadcrumbList elsewhere. No Offer, priceRange, FAQPage or HowTo.
11. Add public/.htaccess (clean URLs, 404 mapping, no directory listing), real placeholder WebP assets under public/img with alt, width and height, deploy/make-zip.ps1 that zips the contents of dist/comida.com.py excluding logs and credentials, and a comida-preview entry (port 8093 serving dist/comida.com.py) in C:\Claude 1\.claude\launch.json without changing existing entries.
12. Write README.md: build, verify, preview, the config path, the CRM contract fields still to be confirmed, and the five PHP test cases to run on Hostinger.

Commands to run before reporting: node engine/build-site.mjs --site=comida and node engine/verify.mjs --site=comida. Both must exit 0. Also print the number of HTML files in dist/comida.com.py and the number of URLs in its sitemap.

Definition of done: report in under 300 words with the list of files created or changed, the HTML and sitemap counts, the verify summary, and Assumptions. Do not paste file contents.
