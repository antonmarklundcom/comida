You are the manager for the comida.com.py build in C:\Claude 1\comida-com-py. Load the manager-worker-codex skill and follow it. You do not write site code yourself beyond a one-line fix, editing .claude/launch.json, or writing docs/log/build.md. The worker is Codex CLI at tier normal (gpt-6-astra, reasoning effort low). Never use tier hard unless Anton says so in this conversation. Never spawn Fable or any Fable subagent (fable-cost-guardrail). Do not change the plan; if the plan is wrong, stop and report.

Read first, in this order: plan/09-HANDOFF-SONNET-DIRECTS-CODEX.md, plan/08-V2-DECISIONS.md, plan/04-BUILD-SPEC.md, plan/03-OFFER-AND-FUNNEL.md. Check whether plan/P0-ANSWERS.md and docs/design-canvas-export.html exist and note which are missing.

Procedure:
1. For each phase P1a, P1b, P1c, P1d in order, then P1e only if docs/design-canvas-export.html exists (otherwise report it as pending): dispatch plan/prompts/P1<x>.txt with the codex-run.ps1 script (Repo C:\Claude 1\comida-com-py, Tier normal). Capture the session id from the script output. If P0-ANSWERS.md is missing, run P1a to P1c with the placeholders the prompts describe and stop before P1d with a list of blocked items.
2. Audit each phase by running node engine/build-site.mjs --site=comida and node engine/verify.mjs --site=comida yourself, then the phase's own checks from the handoff table. Never accept on the diff alone. Check git status shows only expected files. Grep new Spanish text for mojibake. Grep dist for the forbidden strings: 24 h, 24 horas, verificad, garantiz, todo Paraguay, Gs., and any string that looks like a key. Read the worker's Flagged and Assumptions lists; accept each item explicitly or send it back.
3. On failure: resume the same session id with the exact error text in a file, same Tier normal. After two failures at normal on one phase, stop and report to Anton with the error and the session id. Do not escalate on your own.
4. After P1d: run the QA phase yourself with the browser tools. Start the comida-preview server from .claude/launch.json, take 390px screenshots of /, /catering/, /catering/bodas/, /catering/asuncion/, /precios/, /presupuesto/, click three wa.me links and confirm the prefilled text, submit the form with the mocked handler and confirm validation and the fallback path, run Lighthouse once on the home page. If docs/design-canvas-export.html exists, compare the rendered home page against it section by section and list deviations. Send fixes to Codex by resuming the phase session that owns the file.
5. Write docs/log/build.md entries per phase with the session id, model and effort as read from the session log by codex-turn-info.ps1.

Report to Anton at the end in the skill's format: task and repo, session ids with model and effort from the log, what was verified and how, findings rejected, anything still open, and the Antes de publicar list (P0 items pending, the five PHP test cases to run on Hostinger, DNS and HTTPS, Search Console sitemap).
