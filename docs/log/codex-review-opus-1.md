# Review of opus-1: comida.com.py

Review date: 2026-09-19. Scope: ideas only; the export and assets were not changed. Locations below refer to `docs/design-exports/opus-1.html` unless another full file path is given; section names identify the relevant code even if line numbers shift. Proposed engine paths do not exist yet.

Read: `plan/prompts/DESIGN-1.txt`, `plan/08-V2-DECISIONS.md`, `plan/04-BUILD-SPEC.md`, `AGENTS.md`, and `docs/imagery-manifest.json`. This is a source-based review, not a rendered-browser audit; viewport collisions, image content, contrast over photos, and actual network behavior require visual/runtime verification. No claims of measured Lighthouse scores or legal compliance are made.

Labels: **RULE** = observed departure from a hard rule or locked token; **SPEC** = brief/design requirement departure; **RISK** = conditional or needs verification; **BUILD** = production integration work, expected to be absent from a design export. Each numbered item is one concrete change, location, and reason.

## conversion and copy

1. Link “Ver guía de precios” to `/precios/` instead of its own `#precios` section — `docs/design-exports/opus-1.html`, precios teaser, around line 684 — delivers the guide the CTA promises.
2. Link “Sumar mi servicio” to `/proveedores/` instead of the customer contact form — `docs/design-exports/opus-1.html`, proveedores band, around line 725 — prevents supplier applications being mistaken for event requests.
3. Add a short referral explanation beside the hero actions, such as “Te conectamos con proveedores; contratás directamente con ellos,” while preserving the required hero copy — `docs/design-exports/opus-1.html`, hero-copy — establishes who actually supplies the catering before a visitor submits.
4. Clarify that “Gratis” refers to requesting and coordinating quotes, keeping the existing explanatory heading visually attached — `docs/design-exports/opus-1.html`, cómo funciona statement — avoids implying that the catering itself is free.
5. Replace “Él te cotiza, te contrata y te factura” with “El proveedor te cotiza y factura; vos contratás directamente con él” — `docs/design-exports/opus-1.html`, FAQ on invoicing — fixes the misleading suggestion that the supplier hires the customer.
6. Replace “sin que organices todo vos” with “Contanos la fecha y consultamos disponibilidad” — `docs/design-exports/opus-1.html`, Fin de año tile — avoids suggesting full event-planning services beyond referral and coordination.
7. Remove the unsupported “Te lleva un minuto” estimate or substantiate it through usability testing before use — `docs/design-exports/opus-1.html`, first process step, around line 563 — sets an honest effort expectation; this is not a prohibited response-time-in-hours claim.
8. Explain the next step beneath Continuar, for example “Después te pedimos tu nombre y WhatsApp” — `docs/design-exports/opus-1.html`, mini-form hint — makes the two-screen flow predictable without adding fields.
9. Bring the actual published service hours into a small note near the first form — `docs/design-exports/opus-1.html`, mini-form footer — explains “horario de atención” before the visitor reaches the bottom of the page.
10. Qualify the small-events FAQ with availability language rather than an unconditional “Sí” — `docs/design-exports/opus-1.html`, FAQ “¿Atienden eventos chicos?” — keeps the promise aligned with supplier minimums and actual supply.
11. Keep both business and family examples visible early in the occasions section, with Fin de año immediately after Empresariales as requested — `docs/design-exports/opus-1.html`, occasions DOM order — serves both audiences and restores the intended occasion sequence.
12. Use “Pedir presupuesto” consistently for starting a request and reserve “Continuar” for advancing an existing request — `docs/design-exports/opus-1.html`, header, hero, quick form, contact and mobile bar — makes repeated actions easier to distinguish.

## layout and visual design

13. **SPEC:** Remove the standalone appetite strip from between the hero and mini form, retaining any approved food details within the hero media instead — `docs/design-exports/opus-1.html`, lines 475–501 and `.quick` — restores the prescribed hero → mini form → process sequence and shortens the route to conversion.
14. **SPEC:** Restore the requested desktop 6/6 hero composition and 48px headline overlap instead of 32/68 columns and a 160px negative margin — `docs/design-exports/opus-1.html`, desktop `.hero` and `.hero-copy` — gives copy a stable reading width and respects the art direction.
15. Attach the white mini-form panel to the hero/process boundary rather than overlapping the appetite-strip captions — `docs/design-exports/opus-1.html`, `.quick` negative margins — preserves the required cross-section element without hiding disclosures.
16. **RULE:** Remove gold `#D9A441` and char `#1C1511` as new palette tokens; use the locked palette — `docs/design-exports/opus-1.html`, `:root`, formats, providers and footer — prevents unapproved color-system drift.
17. **RULE:** Remove paprika from the brand dot, ordinary nav/link hover states, guide link and checkbox selection; reserve it for primary CTAs and one highlight per viewport — `docs/design-exports/opus-1.html`, `.brand b`, navigation, `.link-arrow`, `.check` and `.tile.cta .go` — restores the locked accent hierarchy.
18. **RULE:** Restore olive eyebrows on light surfaces and place dark-section eyebrows on a small approved light surface if needed for contrast — `docs/design-exports/opus-1.html`, `.formats-band .over .eyebrow` and `.prov .eyebrow` — fixes gold eyebrow substitutions without sacrificing legibility.
19. **RULE:** Restore 14px corners on desktop occasion cards and image containers; replace the 10px card-label corners where they act as card surfaces — `docs/design-exports/opus-1.html`, desktop `.tile`, `.tile .label`, image wrappers — preserves the locked card/image radii rather than switching to square tiles.
20. **RULE:** Normalize ordinary borders to 10% ink opacity, replacing the scattered .18/.20/.28 values; use a distinct accessible focus outline for interaction — `docs/design-exports/opus-1.html`, chips, inputs, ghost buttons and icon button — separates the locked border token from focus affordances.
21. **RULE:** Consolidate ad hoc heading sizes such as 24px and 28px onto the requested 1.25 scale from 17px, retaining the explicitly locked H1 and eyebrow sizes — `docs/design-exports/opus-1.html`, form headings, step headings, tile headings and submenu — makes the type hierarchy consistent.
22. Remove the fixed `"opsz" 144` overrides from headings so `font-optical-sizing:auto` can respond to each displayed size — `docs/design-exports/opus-1.html`, heading and statement font rules — implements the intended optical sizing instead of forcing one optical size everywhere.
23. **SPEC:** Keep the occasions section to seven tiles and move the extra “Otra ocasión” invitation into a plain text link below them — `docs/design-exports/opus-1.html`, eighth `.tile.cta` — preserves the required seven-item grid while retaining the useful catch-all action.
24. Scope FAQ `details`, `summary` and `details p` styles to `.faq` — `docs/design-exports/opus-1.html`, FAQ CSS around lines 300–309 — prevents accordion styling leaking into the mobile navigation disclosure.
25. Replace transparent photo-panel repetition in formats and providers with the specified hairline format row and solid ink provider band — `docs/design-exports/opus-1.html`, `.formats-band` and `.prov` — strengthens section differentiation and reduces visual competition with the hero.

## imagery and art direction

26. **RISK:** Record a clear design exception for photography versus DESIGN-1's tinted-placeholder rule, retaining the supplied images and carousel pending that resolution — `docs/imagery-manifest.json`, `_notes`, and `docs/design-exports/opus-1.html`, all image sections — reconciles the later recorded imagery/carousel request with the original hard wording without silently discarding approved assets.
27. Audit the boda, cobertura and conference assets at every actual crop; pre-crop any remaining faces out of the delivered derivatives — `docs/imagery-manifest.json`, `review_notes`, assets 7, 14 and 18; `docs/design-exports/opus-1.html`, matching pictures — addresses the manifest's explicit face-risk warning rather than assuming CSS positioning is sufficient.
28. Define separate mobile and desktop focal positions for the conference slide and wedding tile — `docs/design-exports/opus-1.html`, slide 2 and Bodas picture — keeps food visible across the very different container shapes.
29. Give every illustrative disclosure a reliably opaque, approved background and keep it outside overlapping panels — `docs/design-exports/opus-1.html`, `.chip-cap`, `.cap`, quick-form overlap — makes the disclosure readable over both bright and dark food imagery.
30. Rebalance the first visible imagery toward buffet, chipa and shared corporate service rather than repeated meat close-ups — `docs/design-exports/opus-1.html`, hero, appetite strip and formats — broadens relevance for office managers and families without adding unsupported dietary claims.
31. Replace the delivery-oriented slide caption with an event-service description only if the depicted service supports it; otherwise use another existing event image — `docs/design-exports/opus-1.html`, third hero slide — avoids attracting ordinary food-delivery demand that v2 defers.
32. Use a neutral food/detail image instead of the branded-service implication of a catering van, or clarify that it depicts an illustrative setting — `docs/design-exports/opus-1.html`, cobertura image and alt — reduces the impression that comida.com.py owns vehicles or directly delivers catering.
33. Audit generated notebooks, bottle labels, uniforms and backgrounds for readable marks or logos before release — `docs/imagery-manifest.json`, contact, cumpleaños and provider assets — prompts alone do not establish compliance with the no-logo/no-readable-text art direction.
34. Extend imagery records with derivative filenames, actual dimensions and approved focal crops — `docs/imagery-manifest.json`, image entries — lets templates use the delivered assets consistently rather than treating generated source dimensions as final dimensions.
35. Choose a relevant buffet/service image for the prices teaser if no chef partner is available — `docs/design-exports/opus-1.html`, precios picture — keeps the pricing guide visually representative of the service being launched.

## mobile

36. Put hero copy before media in both DOM and mobile visual order, retaining the desktop asymmetric split — `docs/design-exports/opus-1.html`, `.hero` children and mobile CSS — lets phone visitors understand the offer before scrolling through a minimum 380px image.
37. **SPEC:** Replace the mobile occasions carousel with one-column cards; preserve horizontal snapping only for the expressly requested formats row — `docs/design-exports/opus-1.html`, `.bento` below 1100px — makes all seven occasions discoverable and meets the one-column mobile direction.
38. **SPEC:** Eliminate the three-column mobile appetite grid as part of folding it into the hero — `docs/design-exports/opus-1.html`, `.strip` — avoids tiny food labels and the brief's one-column conflict.
39. **RULE:** Increase carousel dot buttons from 28px to at least 48px while retaining small visual dots — `docs/design-exports/opus-1.html`, `.dots button` — meets the explicit mobile target requirement.
40. **RULE:** Increase the date-flexible label row from 32px to at least 48px — `docs/design-exports/opus-1.html`, `.check` — makes the whole checkbox label comfortably tappable.
41. **RULE:** Increase footer link targets from 44px to 48px on mobile — `docs/design-exports/opus-1.html`, `.footer nav a` — brings legal and navigation links up to the requested target size.
42. Reserve page-bottom space equal to the actual mobile bar height plus `env(safe-area-inset-bottom)` rather than a fixed 76px — `docs/design-exports/opus-1.html`, body and `.mbar` — prevents the last content being obscured on devices with bottom insets.
43. Add sticky-header scroll offsets to `#pedir`, `#contacto`, `#precios` and `#como-funciona` — `docs/design-exports/opus-1.html`, anchor targets — keeps headings and form entry points visible after CTA navigation.
44. Let the contact WhatsApp block wrap or stack its hours and action at narrow widths — `docs/design-exports/opus-1.html`, `.wa-block` — avoids squeezing a glyph, multi-line hours and a button into a single small row.
45. Remove decorative empty `.check` spacer spans from the mobile field layout and use desktop grid alignment instead — `docs/design-exports/opus-1.html`, quick form around lines 536–545 — removes unnecessary vertical gaps before Continuar.
46. Test the desktop header and quick-form grid at 900–1099px; postpone the full nav or reduce columns when their contents do not fit — `docs/design-exports/opus-1.html`, 900px breakpoints — addresses the large jump to a crowded desktop layout on narrow tablets.
47. Use dynamic viewport sizing for the drawer and coordinate its open state with the sticky bottom bar — `docs/design-exports/opus-1.html`, `.drawer`, `.mbar`, mobile menu script — keeps navigation usable as browser chrome and the keyboard change available height.
48. Reposition mobile slide captions and arrow controls into non-overlapping rows at 390px — `docs/design-exports/opus-1.html`, `.slide-cap` and `.car-ctrl` — avoids long captions competing with two 48px buttons across the same top edge.
49. Replace `overflow-x:hidden` as a masking strategy with specific overflow fixes, then check document width at 320px, 390px and text zoom — `docs/design-exports/opus-1.html`, body and responsive layouts — exposes clipped content instead of concealing layout defects.

## accessibility

50. Add a visible-on-focus “Ir al contenido” link targeting main — `docs/design-exports/opus-1.html`, immediately after body opening — lets keyboard users bypass repeated navigation.
51. Give the carousel a persistent pause/resume button and retain the user's paused choice across hover and visibility changes — `docs/design-exports/opus-1.html`, `.car-ctrl` and `start/stop` script — makes automatic motion controllable without removing the requested carousel.
52. Listen for changes to `prefers-reduced-motion`, not just its initial value, and stop the timer immediately when enabled — `docs/design-exports/opus-1.html`, carousel script — respects motion preferences changed during a visit.
53. Restrict Escape handling to an open desktop submenu; handle the mobile drawer separately and restore focus to its own trigger — `docs/design-exports/opus-1.html`, document keydown handler — prevents Escape anywhere on mobile from focusing the hidden desktop Catering button.
54. Close the desktop submenu after its in-page CTA is followed — `docs/design-exports/opus-1.html`, `.sub` links and dropdown script — prevents the open overlay obscuring the form destination.
55. Give the mobile drawer a navigation landmark and an appropriate expanded-state accessible name for its trigger — `docs/design-exports/opus-1.html`, `.mnav summary` and `.drawer` — makes “Abrir menú” less confusing when the menu is already open.
56. Supply a clearly visible high-contrast focus indicator for form inputs rather than only the faint paprika box-shadow after `outline:none` — `docs/design-exports/opus-1.html`, `.field input:focus` and `.field select:focus` — makes keyboard position easier to locate.
57. Use `aria-describedby` for hints and field errors and `aria-invalid` only on fields that have failed validation — `docs/design-exports/opus-1.html`, both forms — connects corrective instructions with their controls.
58. Add a focusable submission-error summary with links to the affected fields — `docs/design-exports/opus-1.html`, contact form — helps users recover without searching through two screens.
59. Expose pending, failure and confirmed-success status through an appropriate live status region — `docs/design-exports/opus-1.html`, contact form footer — communicates asynchronous results without relying on color or button text alone.
60. Make the non-interactive formats scroller keyboard reachable with an accessible region label and visible focus — `docs/design-exports/opus-1.html`, `.fmt-row` — lets keyboard users discover all six horizontally arranged formats.
61. Announce manual carousel changes with a concise current-position status without repeatedly announcing automatic changes — `docs/design-exports/opus-1.html`, slides and dots — helps screen-reader users understand their navigation actions.
62. Calculate contrast for every text/background combination, including photo captions under worst-case image brightness, and adjust surfaces rather than abandoning locked colors — `docs/design-exports/opus-1.html`, `.chip-cap`, `.tile .label`, dark bands — verifies the explicit 4.5:1 body-copy requirement.
63. Keep DOM and visual reading order aligned in the dense bento or remove `grid-auto-flow:dense` when it reorders tiles — `docs/design-exports/opus-1.html`, desktop `.bento` — prevents keyboard focus from appearing to jump unpredictably.

## performance

64. Replace the stale preload for `mesa-buffet-catering-evento-asuncion` with the actual first-slide `catering-buffet-evento-salon-asuncion` source set — `docs/design-exports/opus-1.html`, head line 10 and hero line 426 — avoids prioritizing a retired image absent from the page.
65. Correct occasion image `sizes` from 66vw/33vw to the actual two-of-five/one-of-five desktop widths, including gaps — `docs/design-exports/opus-1.html`, occasions source elements — prevents browsers choosing unnecessarily large images.
66. Match mobile occasion `sizes` to the final one-column card width with 16px gutters — `docs/design-exports/opus-1.html`, occasions pictures — preserves responsive image savings after the layout correction.
67. If appetite thumbnails remain inside the approved hero treatment, supply suitably small derivatives rather than making 640px the minimum source for roughly 130px thumbnails — `docs/design-exports/opus-1.html`, appetite pictures; `assets/img/` — reduces unnecessary phone image bytes.
68. Keep only the first hero slide at high priority and verify when neighboring lazy slides actually download — `docs/design-exports/opus-1.html`, hero pictures — prevents assuming native lazy loading saves bandwidth for immediately adjacent carousel content.
69. Pause carousel work while the hero is outside the viewport, in addition to the existing tab-visibility pause — `docs/design-exports/opus-1.html`, carousel script — avoids repeated offscreen animation during form completion.
70. Coalesce scroll updates behind a single pending animation frame — `docs/design-exports/opus-1.html`, track scroll listener — avoids scheduling multiple redundant dot-state updates within the same frame.
71. Move shared CSS and JS into cacheable local production assets when integrating, retaining a small critical style block only if measured beneficial — `docs/design-exports/opus-1.html`, inline style/script; planned `public/` — avoids resending all shared code across the launch routes.
72. Bundle approved font files locally with only needed faces and `font-display:swap`, acquired outside the build step — `docs/design-exports/opus-1.html`, Google Fonts links; planned `public/` fonts — reduces third-party runtime dependencies without adding forbidden build-time network calls.
73. Measure the sticky header's backdrop filter on a representative Android device and replace it with an opaque locked base surface if costly — `docs/design-exports/opus-1.html`, `.header` — protects scrolling responsiveness on the stated primary device class.
74. Package only production-used derivatives, excluding the retired hero and generation metadata — planned engine asset copy from `assets/img/` — keeps the public bundle focused without deleting source assets.

## SEO and structured data

75. **BUILD:** Add the absolute home canonical `https://comida.com.py/` — `docs/design-exports/opus-1.html`, head — satisfies the verifier and establishes the production URL.
76. Consider a more focused title such as “Catering en Gran Asunción | comida.com.py” if the chef service is not launched — `docs/design-exports/opus-1.html`, title — prioritizes the available service; the current title is exactly 60 characters and already meets the editorial limit.
77. Add Open Graph metadata with a local illustrative image and matching descriptive alt — `docs/design-exports/opus-1.html`, head — gives shared links a deliberate preview without ratings, partner branding or prices.
78. Generate home Organization and WebSite JSON-LD from confirmed operator/config data — planned home template and `sites/comida/site.config.mjs`, following `plan/04-BUILD-SPEC.md` §7 — adds factual identity data without inventing a restaurant or physical venue.
79. Keep Service/BreadcrumbList on the applicable service routes and omit Offer, priceRange, FAQPage and HowTo — planned template schema generation, `plan/04-BUILD-SPEC.md` §7 — follows the stated schema policy rather than adding attractive but disallowed markup.
80. Turn footer navigation into real published route links, including `/terminos/`, `/privacidad/`, `/sobre/` and `/contacto/` where appropriate — `docs/design-exports/opus-1.html`, footer — gives visitors and crawlers useful destinations instead of placeholders.
81. Keep a direct crawlable `/catering/` entry visible as well as the occasion submenu — `docs/design-exports/opus-1.html`, header navigation — strengthens access to the generic hub identified as a priority in v2.
82. Link “Asunción” to `/catering/asuncion/` while retaining other cities as plain text — `docs/design-exports/opus-1.html`, coverage list — supports the one approved city route without generating backlog city pages.
83. Derive sitemap count from published and indexable route records, excluding gracias, 404 and any gated chef route — planned `sites/comida/routes.mjs` and sitemap builder — avoids relying on the manifest heading's ambiguous fixed count.
84. Keep this design export outside the deployment copy and public sitemap — planned deploy/build allowlist, `docs/design-exports/opus-1.html` — prevents draft placeholders and duplicated home content from becoming public landing pages.
85. Preserve the exact required H1 and use unique occasion-page titles and descriptions downstream — planned home and occasion templates — protects the brief while avoiding templated metadata duplication.

## legal and compliance with the forbidden-content rules

86. **RULE:** Remove “Si tu evento es en otro lugar, preguntanos igual” or explicitly restrict that invitation to locations within Gran Asunción — `docs/design-exports/opus-1.html`, cobertura lead, around line 695 — the unqualified wording invites requests beyond the locked coverage boundary.
87. **RISK:** Resolve the photo-versus-tinted-placeholder discrepancy explicitly rather than marking every photo automatically approved or automatically forbidden — `plan/prompts/DESIGN-1.txt`, imagery rule; `docs/imagery-manifest.json`, later recorded approvals — the current request acknowledges real images and the manifest records a later carousel request, but neither establishes that every original design constraint was waived.
88. **BUILD:** Replace `OPERADOR_PENDIENTE` with the confirmed legal operator and required identity details before release — `docs/design-exports/opus-1.html`, footer legal line — removes an obvious draft identity placeholder.
89. **BUILD:** Add concise data-use disclosure beside submission, linked to the actual privacy policy, explaining contact and sharing with catering providers — `docs/design-exports/opus-1.html`, contact form footer — makes the intended lead handoff understandable before personal data is sent.
90. Review the provider-selection FAQ against the actual documented checks and remove any check not performed — `docs/design-exports/opus-1.html`, final FAQ — avoids converting aspirational screening into a factual claim.
91. Gate chef navigation on a signed partner and review chef claims in title, hero, formats and footer against the same supply evidence — `docs/design-exports/opus-1.html`, all chef mentions; `plan/08-V2-DECISIONS.md`, chef decision — the absent nav item is correct if no partner is signed, but unconditional service promotion may still overstate supply.
92. Keep quoted prices, commission thresholds and internal price datasets out of rendered HTML and client bundles — planned templates and asset serialization, `plan/08-V2-DECISIONS.md` accepted decisions 7–8 — enforces the hard no-price rule even when internal reference data exists.
93. Have the operator's local legal reviewer confirm the required policy content and commencement issues named in v2 before publishing legal pages — planned `/terminos/` and `/privacidad/`, `plan/08-V2-DECISIONS.md` decision 15 — treats the plan's legal references as review inputs, not as verified legal advice.
94. Keep customer terms explicit about who quotes, contracts, invoices and handles service incidents — planned terms page linked from the footer, informed by the existing referral disclaimer — aligns the customer journey with the actual intermediary model.
95. Add rendered-text checks for prohibited claims, testimonials, ratings, review counts and provider names, while allowing guest counts and published opening hours — planned verifier, using `AGENTS.md` and DESIGN-1 — prevents false positives on “50 o 100 personas,” “15 años” and legitimate service hours.
96. Keep WhatsApp green confined to actual button glyphs; change the standalone green glyph in `.wa-block` to olive or move it inside the WhatsApp button — `docs/design-exports/opus-1.html`, contact WhatsApp block — fixes the literal “only inside the WhatsApp button glyph” token restriction.

## form and lead capture

97. Replace the mini form's GET-to-fragment submission with a real two-step flow that preserves occasion, date/flexibility, zone and guest count — `docs/design-exports/opus-1.html`, quick form around line 505 — the current GET reload does not populate the separate contact form.
98. Use the same canonical occasion field/value mapping in both forms instead of `ocasion` versus `c-ocasion` — `docs/design-exports/opus-1.html`, both radio groups — prevents lost or inconsistently classified occasion data.
99. Keep a compact editable event summary above contact details after Continuar rather than requiring all event fields to be entered again — `docs/design-exports/opus-1.html`, contact form — reduces duplicate effort while allowing corrections.
100. **BUILD:** Replace `action="#"` with the actual PHP handler and a working non-JavaScript submission path — `docs/design-exports/opus-1.html`, contact form — turns the design into a dependable lead-capture path.
101. Require occasion, zone, positive integer guests, name and a usable phone number; require either a date or an explicit flexible-date choice — `docs/design-exports/opus-1.html`, both forms — prevents empty or unusable requests while preserving flexibility.
102. Make date and flexible-date interactions explicit and reject contradictory states consistently on client and server — planned form JS and `php/lead-forward.php` — ensures the operator receives an interpretable event date.
103. Normalize local Paraguayan phone input to the CRM's expected format while accepting spaces and punctuation — `docs/design-exports/opus-1.html`, `#c-wa`; planned PHP handler — reduces unnecessary phone-format errors.
104. Replace every dummy WhatsApp destination with one confirmed config-driven number — `docs/design-exports/opus-1.html`, all `wa.me/595000000000` links — fixes every secondary conversion route at once.
105. Keep typed form data intact on validation or upstream failure and offer a deliberate retry — planned form JS and PHP response handling — prevents users abandoning after re-entering the same brief.
106. Show a disabled pending submit state and use a submission idempotency key that survives retry — `docs/design-exports/opus-1.html`, contact submit; planned PHP handler — reduces duplicate CRM leads without discarding legitimate retries.
107. Show success and navigate to `/gracias/` only after confirmed CRM persistence — planned PHP handler and form JS, `plan/04-BUILD-SPEC.md` §5 — avoids falsely reporting a received request after a timeout or rejection.
108. Offer a WhatsApp fallback containing the user's brief only after explicit user action, excluding that text from analytics and internal navigation URLs — planned failure state — supports recovery while respecting the plan's limits on event/URL data and its prescribed WhatsApp fallback.
109. Keep budget optional and defer detailed menu, dietary and venue questions to qualification — planned form schema, `plan/08-V2-DECISIONS.md` decision 10 — preserves the deliberately short first-contact flow.
110. Add the prescribed server-side allowlist, request-size limit, persistent rate limiting and honeypot handling — planned `php/lead-forward.php` — makes validation more than a client-side visual feature.
111. Implement `form_start`, confirmed `form_submit` and `wa_click` with non-personal placement/occasion identifiers only — planned form/analytics JS — measures conversion quality without collecting phone numbers or brief text in analytics.
112. Map supplier applications to a separate form and `proveedor` lead type, keeping home event requests as `cliente` — planned `/proveedores/` form and PHP mapping — maintains clean downstream qualification and routing.

## build integration with the engine

113. Implement the `sites/comida/` adapter and loader registration before porting the export markup — planned engine/config modules, `plan/04-BUILD-SPEC.md` §1 — the reviewed workspace currently has no engine to receive the design.
114. Extract reusable header, footer, form, CTA and image rendering into vanilla static template helpers — planned `templates/` — keeps the home design consistent across routes without introducing a framework.
115. Move brand, WhatsApp, service hours, operator identity, coverage and nav into `sites/comida/site.config.mjs` — planned site config — eliminates inconsistent hard-coded repetitions and placeholders.
116. Generate occasion links and cards from stable occasion IDs and the route manifest, rejecting unpublished related destinations — planned `sites/comida/occasions.mjs` and `routes.mjs` — prevents route drift between submenu, tiles and contextual links.
117. Rewrite export-relative `../../assets/img/` URLs to deployed image paths through an asset helper — `docs/design-exports/opus-1.html`, all pictures; planned asset-copy code — prevents correct preview paths becoming broken production paths.
118. Share a single token stylesheet and component selectors while preserving the original export as a reference — planned `public/` CSS and home template — lets implementation evolve without treating the design snapshot as production infrastructure.
119. Make chef publication, navigation visibility and sitemap eligibility use one signed-partner gate — planned line data, routes and navigation generation — applies v2 consistently instead of hiding only one link.
120. Replace inherited finance-specific verifier rules with comida checks, retaining escaping, identity, routing and asset checks — planned `engine/verify.mjs`, following build spec §8 — allows the authorized forms/PHP while preventing content and deployment regressions.
121. Add checks for stale preloads, dummy WhatsApp numbers, placeholder operator identity, self-linking destination CTAs and missing image derivatives — planned verifier — catches the concrete design-export failures found in this review.
122. Copy the one PHP handler explicitly, keep its secret config at the documented private absolute path, and never serialize CRM credentials into browser assets — planned build/deploy and `php/lead-forward.php` — matches the prescribed static-plus-PHP architecture.
123. Exercise mocked UI states in preview and the specified success, rejection, timeout, duplicate and missing-config cases on Hostinger — planned preview and deployment verification — separates visual testing from actual PHP/CRM persistence testing.
124. Preserve the no-network build rule by using checked-in image/font artifacts and deterministic local asset copying — planned engine build pipeline — keeps rebuilds reproducible and avoids runtime dependency on image-generation services.
125. Keep deployment packaging allowlisted to production HTML/CSS/JS/images/PHP and `.htaccess`, excluding docs, research and private config — planned `deploy/make-zip.ps1` — prevents this review and generation records from being shipped publicly.
126. Capture desktop 1440px and mobile 390px views for manager review once a browser preview exists; use one responsive production DOM rather than duplicating both frames — planned design QA for `docs/design-exports/opus-1.html` — supplies the original brief's comparison evidence without duplicating forms or content in production.

## Assumptions

- The supplied export is intentionally not production-functional; BUILD findings are integration requirements rather than accusations of a broken live site.
- Later imagery and carousel requests recorded in the manifest justify reviewing the supplied photos/carousel, but do not waive palette, coverage, no-face or disclosure rules by implication.
- The user requests ideas only, so proposed corrections have not been implemented and no other files have been created.
- Guest counts, numbered process steps and opening hours are not price numbers or response-time guarantees; existing occurrences are not flagged merely for containing digits.
- The prompt calls the mini form “five fields” but enumerates four data groups plus Continuar; “Cuatro datos” is therefore not independently a rule violation.
- The signed-chef status, final operator details, supplier screening evidence and live CRM contract were not supplied or validated in this review.

## Flagged

- Confirmed hard-rule/token departures: unqualified outside-area invitation; additional gold/char palette; paprika outside its permitted roles; gold eyebrows; non-token card/image corners and borders; off-scale typography; mobile 28px dots, 32px flexible-date row and 44px footer targets; green WhatsApp glyph outside its button.
- Design/spec departures: appetite strip interrupts the required hero/form sequence; 32/68 hero with 160px overlap differs from 6/6 with 48px overlap; eight occasion tiles instead of seven; mobile occasions are a horizontal carousel and appetite images remain three columns; desktop and mobile stacked review frames are not delivered by this responsive HTML alone.
- Conditional issues: photo treatment conflicts with the initial tinted-block wording but later photography/carousel work is recorded; manifest explicitly identifies potential faces in boda, cobertura and conference images, requiring rendered crop inspection; chef-nav omission depends on the signed-partner gate, so it is not declared an unconditional violation.
- No prohibited price amount, fixed quote-count guarantee, response-time-in-hours guarantee, prohibited verification/guarantee term, testimonial or review count was found in the HTML copy. Pixel-level logos/faces were not verified. The supplied text brand and functional WhatsApp symbol are expressly requested UI elements, not fabricated partner endorsements.
- Build attempted: `node engine/build-site.mjs --site=comida` exited 1 because `engine/build-site.mjs` is absent. Verify attempted: `node engine/verify.mjs --site=comida` exited 1 because `engine/verify.mjs` is absent. Node is available (v24.19.0); building an engine would exceed this review-only dispatch.
- `git status --short` failed because the supplied directory is not a Git repository; a clean Git diff cannot be certified. `rg` is unavailable, so PowerShell reads/searches were used.
- Counts: 126 improvement ideas across the 10 requested categories; one review file created. The imagery manifest contains 19 entries; `assets/img` contains 85 files totaling 5,632,801 bytes, which is a source-directory total, not a measured page transfer size.
